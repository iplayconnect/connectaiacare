Flask==3.0.3
gunicorn==22.0.0
python-dotenv==1.0.1

psycopg2-binary==2.9.9

# google-genai (SDK moderno, suporta Gemini 3 + Live API + TTS).
# Substituiu google-generativeai legacy (que não tinha thinking_config nem
# response_modalities=AUDIO).
google-genai>=0.6.0,<2.0.0

httpx==0.27.2
requests==2.32.3

# Voz Live API (Sofia.4): WebSocket bidirecional via uvicorn + websockets.
uvicorn[standard]==0.32.0
websockets>=12.0
fastapi==0.115.0

structlog==24.4.0
supervisor==4.2.5
