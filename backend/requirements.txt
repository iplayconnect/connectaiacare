Flask==3.0.3
Flask-CORS==4.0.1
gunicorn==22.0.0
python-dotenv==1.0.1

psycopg2-binary==2.9.9
redis==5.0.8
SQLAlchemy==2.0.35
alembic==1.13.3

anthropic==0.40.0
google-generativeai==0.8.3
deepgram-sdk==3.7.0
openai==1.54.0

requests==2.32.3
httpx==0.27.2
pydantic==2.9.2

python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt explicit — chamado diretamente em src/services/user_service.py
# (passlib 1.7.4 tem bug de compat com bcrypt 5.x: detect_wrap_bug).
bcrypt==5.0.0

pillow==10.4.0
pydub==0.25.1
numpy==1.26.4

python-multipart==0.0.12
aiofiles==24.1.0

# Voice biometrics (Resemblyzer + pgvector)
# setuptools: Python 3.12 removeu pkg_resources do stdlib implícito.
# webrtcvad (dep indireta do resemblyzer) ainda depende de `import pkg_resources`.
# ATENÇÃO: setuptools >=80 REMOVEU pkg_resources do pacote — pinamos <80.
# Ver https://github.com/pypa/setuptools/issues/4935
setuptools>=75.0.0,<80
resemblyzer==0.1.4
scipy==1.14.1
torch==2.4.1
pgvector==0.3.6

structlog==24.4.0
rich==13.9.2

# Teleconsulta via LiveKit (ADR-012 — reuso LiveKit da Hostinger)
# SDK gera JWT tokens + cria rooms via API. Web clients usam livekit-client (npm).
livekit-api==0.8.2

# PDF da receita médica + QR de verificação (CFM 2.314/2022).
reportlab==4.2.5
qrcode==7.4.2

# LLM Router — config YAML por tarefa (ADR-025)
PyYAML==6.0.2
